import { Datenschutz } from "@/components";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Datenschutz | Handwerk Ecke",
};

export default function DatenschutzPage() {
  return <Datenschutz />;
}
